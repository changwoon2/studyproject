const name = "List";
const age = "30";

if (name === "List" && age > 19) {
  console.log("통과");
} else {
  console.log("퇴장");
}

const gender = "M";
const open = "Jane";
const isAdult = true;

if(gender === "M" && (open === "Mike") || isAdult) {
  console.log("통과");
} else {
  console.log("퇴장");
}
